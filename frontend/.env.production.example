# 生产环境配置文件示例
# 复制此文件为 .env.production 并根据实际部署环境修改

# API 基础 URL（替换为您的域名或服务器 IP）
# 使用 HTTPS 的示例:
VITE_API_BASE_URL=https://yourdomain.com/api/v1

# WebSocket 基础 URL（替换为您的域名或服务器 IP）
# 使用 WSS (WebSocket Secure) 的示例:
VITE_WS_BASE_URL=wss://yourdomain.com/ws

# 如果没有域名，使用服务器 IP（HTTP）:
# VITE_API_BASE_URL=http://your-server-ip/api/v1
# VITE_WS_BASE_URL=ws://your-server-ip/ws

# 注意事项:
# 1. 如果使用 HTTPS (https://), WebSocket 必须使用 WSS (wss://)
# 2. 如果使用 HTTP (http://), WebSocket 使用 WS (ws://)
# 3. 域名或 IP 必须与后端 CORS_ORIGINS 配置匹配
# 4. 修改后需要重新构建前端: npm run build
