# Spreadsheet Normalizer Configuration


# LLM Configuration
llm:
  llm_provider: 'gemini'  # ← 改这里！
  model: 'gemini-2.0-flash'  # ← 改这里！
  temperature: 0
  max_tokens: 4000

# Encoder Configuration (SpreadsheetLLM)
encoder:
  anchor_neighborhood: 2  # K parameter for structural anchors

# Structure Analyzer Configuration
analyzer:
  # 现有配置
  detect_headers: true
  detect_aggregates: true
  detect_implicit_aggregates: true
  
  # 🔥 颜色检测配置（专门为你的格式优化）
  use_color_detection: true
  color_threshold: 0.1      # 10% 即可（你的数据通常每行只有 1 个高亮单元格）
  min_colored_cells: 1      # 至少 1 个单元格
  
  llm_provider: "gemini"
  model: "gemini-2.0-flash-exp"

# Schema Estimator Configuration
estimator:
  standardize_names: true
  detect_types: true
  merge_similar_columns: false
  detect_composite_columns: true
  split_composite_columns: true
  preserve_bilingual: true

# Transformation Generator Configuration
generator:
  max_retries: 3
  validate_output: true
  preserve_data: true

# Output Configuration
output:
  format: csv
  encoding: utf-8
  index: false

# Logging Configuration
logging:
  level: INFO
  save_intermediate: true
  output_dir: output