# 后端环境变量配置文件示例
# 复制此文件为 .env 并根据实际情况修改

# ==================== Python 配置 ====================
# Python 路径配置（用于导入 spreadsheet-normalizer 模块）
PYTHONPATH=../spreadsheet-normalizer

# ==================== 存储配置 ====================
# 上传文件存储目录
UPLOAD_DIR=./storage/uploads

# 标准化输出文件存储目录
OUTPUT_DIR=./storage/outputs

# ==================== 会话配置 ====================
# 会话超时时间（秒）
SESSION_TIMEOUT=3600

# 最大文件大小（字节，默认 100MB）
MAX_FILE_SIZE=104857600

# ==================== CORS 配置 ====================
# 允许的前端源地址（多个地址用逗号分隔）
# 生产环境示例:
# CORS_ORIGINS=https://yourdomain.com,http://yourdomain.com
# 开发环境:
CORS_ORIGINS=http://localhost:5173

# ==================== LLM API 配置 ====================
# 根据使用的 LLM 提供商，配置相应的 API Key

# Gemini (通过 API2D)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_BASE_URL=https://oa.api2d.net/v1

# API2D (Claude 等)
API2D_API_KEY=your_api2d_key_here
API2D_BASE_URL=https://oa.api2d.net/v1

# Qwen (阿里云通义千问)
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# DeepSeek (可选)
# DEEPSEEK_API_KEY=your_deepseek_api_key_here
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== 注意事项 ====================
# 1. 此文件包含敏感信息，切勿提交到 Git 仓库
# 2. 修改后需要重启后端服务才能生效
# 3. CORS_ORIGINS 必须包含前端访问地址
# 4. 至少配置一个 LLM API Key（根据 config.yaml 中的 llm_provider 选择）
# 5. 生产环境建议使用环境变量管理工具（如 Docker secrets, AWS Secrets Manager）
